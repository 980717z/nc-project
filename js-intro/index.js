/*
 * This file provides some incomplete functions for you
 * to practice javascript syntax. Complete the required functions
 * based on comments and doc string, then run `npm test` to 
 * test your code.
 */


/**
* Return the product of num1 and num2
*/
exports.multiply = (num1=0, num2=0) => {
  
}


/**
 * Return the sum of arr1 and arr2. The sum of two arrays, 
 * arr1 and arr2, is an array arr3 such that for some integer i,
 * arr3[i] = arr1[i] + arr2[i].
 * 
 * If arr1 and arr2 have the same length, the resulting array should have
 * that length as well. If arr1 and arr2 have different lengths, the 
 * length of the resulting array is min(arr1.length, arr2.length).
 * If arr1 and arr2 are empty, an empty array should be returned.
 * 
 * Precondition: arr1 and arr2 are array of integers.
 */
exports.sumArrays = (arr1=[], arr2=[]) => {

}
